{
  "name": "@authtoolkit/loyalty-passes",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first API foundation for AuthToolkit Loyalty Passes.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test dist/tests/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "license": "UNLICENSED"
}
